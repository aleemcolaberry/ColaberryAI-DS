On/off toggle with a spring knob (leaf-green when on) — settings, preferences.

```jsx
<Switch label="Email me about new cohorts" defaultChecked />
<Switch label="SMS alerts" />
```

Behaves like a checkbox (`checked`, `defaultChecked`, `onChange`, `disabled` pass through) with `role="switch"`.
