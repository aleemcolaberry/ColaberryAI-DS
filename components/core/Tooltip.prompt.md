Hover/focus tooltip for icon buttons and terse hints.

```jsx
<Tooltip label="Closes the menu"><Button variant="ghost">×</Button></Tooltip>
```

`placement`: top (default), bottom, left, right. Wraps a single trigger; appears on hover or keyboard focus.
