Sliding carousel with prev/next controls and dot indicators — testimonials, feature highlights.

```jsx
<Carousel slides={[<Slide1/>, <Slide2/>, <Slide3/>]} />
```

`slides` is an array of nodes. `loop` (default true) wraps at the ends. Dots and arrows appear automatically when there's more than one slide.
